{
  "name": "ziam-chaincode",
  "version": "0.1.0",
  "private": true,
  "description": "Hyperledger Fabric chaincode — IdentityContract + AuditContract for the Blockchain-Enhanced Identity Verification prototype (ROADMAP Phase 5). Deployed to and executed by the Fabric peers, NOT by the Express backend — hence its own package.json.",
  "license": "MIT",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "fabric-chaincode-node start",
    "test": "node test/contracts.test.js"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.4",
    "fabric-shim": "^2.5.4"
  }
}
