/** Portal handlers (courses / fees / results) — skeleton. */
import type { Request, Response } from 'express'

export function getCourses(_req: Request, res: Response): void {
  res.json({ status: 'not_implemented' })
}
// TODO: getFees, getResults
