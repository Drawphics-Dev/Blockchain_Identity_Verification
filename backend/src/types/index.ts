/** Shared domain and Zero Trust types — skeleton. */

export type Decision = 'ALLOW' | 'STEP_UP' | 'DENY' | 'TERMINATE'

// TODO: Student, Course, FeeStatement, SemesterResult, RiskContext,
//       RiskAssessment, Session, AuditEvent, ...
